"""overnight-options-desk — spike scaffolding for the desk pipeline.

`solari_client` wraps the parts of the Solari SDK the four downstream lanes
will need (browser open/read, sandbox exec, port preview). `contracts` defines
the data shapes those lanes hand each other.
"""
